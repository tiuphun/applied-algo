#include <iostream>
using namespace std;
#define N 5009
int n;
int k;
pair<int, int> ref[N];

int main(int argc, char const *argv[])
{
    cin >> n >> k;
    for (int i = 0; i < k; i++) cin >> ref[i].first >> ref[i].second;
    
    return 0;
}
